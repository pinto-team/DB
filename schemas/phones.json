{
  "$id": "https://yourdomain/schemas/phone.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Phone",
  "type": "object",
  "additionalProperties": false,

  "properties": {
    "id": { "type": "string", "format": "uuid", "readOnly": true },
    "owner_type": {
      "type": "string",
      "minLength": 1,
      "description": "Type of entity that owns the phone number (e.g., merchant, customer, order)"
    },
    "owner_id": {
      "type": "string",
      "format": "uuid",
      "description": "Identifier of the owning entity"
    },
    "label": { "type": "string", "minLength": 1, "description": "Label (e.g., Support, Sales)" },
    "country_code": {
      "type": ["string", "null"],
      "pattern": "^\\+[0-9]{1,4}$",
      "description": "E.164 country code (e.g., +98)"
    },
    "number": {
      "type": "string",
      "pattern": "^[0-9]{5,15}$",
      "description": "Phone number (digits only)"
    },
    "type": {
      "type": "string",
      "enum": ["mobile", "landline", "whatsapp", "support", "manager", "fax", "emergency", "sales"],
      "default": "mobile"
    },
    "is_primary": { "type": "boolean", "default": false },
    "is_verified": { "type": "boolean", "default": false },
    "verified_at": { "type": ["string", "null"], "format": "date-time" },
    "status": {
      "type": "string",
      "enum": ["active", "inactive", "suspended"],
      "default": "active"
    },
    "created_at": { "type": "string", "format": "date-time", "readOnly": true },
    "updated_at": { "type": "string", "format": "date-time", "readOnly": true },
    "deleted_at": { "type": ["string", "null"], "format": "date-time", "readOnly": true }
  },

  "required": ["owner_type", "owner_id", "label", "number", "type", "status"],

  "x-pg": {
    "table": "phones",
    "primaryKey": ["id"],
    "unique": [
      { "columns": ["owner_type", "owner_id", "number", "type"], "where": "deleted_at IS NULL" }
    ],
    "indexes": [
      { "name": "idx_phones_owner", "columns": ["owner_type", "owner_id"] },
      { "name": "idx_phones_type", "columns": ["type"] },
      { "name": "idx_phones_status", "columns": ["status"] },
      { "name": "idx_phones_is_primary", "columns": ["is_primary"] }
    ],
    "triggers": [
      { "name": "trg_phones_set_updated_at", "timing": "BEFORE", "event": "UPDATE", "function": "set_updated_at()" }
    ]
  }
}
